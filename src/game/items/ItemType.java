package game.items;
/**
 * Enum representing different types of items in the game.
 * <p>
 * Currently, this enum includes a single item type: SHADOW_TREE.
 * More item types can be added as needed.
 */
public enum ItemType {
    /**
     * Represents the Shadow Tree item type.
     */
    SHADOW_TREE,
    REMEMBRANCE,
}
